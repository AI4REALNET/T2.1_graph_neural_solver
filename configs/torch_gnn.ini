[DEFAULT]
name = "torch_gnn"
layers = (300, 300, 300, 300)
activation = "relu"
layer = "linear"
input_dropout = 0.0
dropout = 0.0
metrics = ("MAELoss",)
loss = {"name": "MSELoss",
        "params": {"size_average": None,
                   "reduce": None,
                   "reduction": 'mean'}}
device = "cpu"
optimizer = {"name": "adam",
             "params": {"lr": 3e-4}}
train_batch_size = 128
eval_batch_size = 128
epochs = 10
shuffle = False
save_freq = False
ckpt_freq = 50

[CONFIG1]
input_dim = 1
hidden_dim = 100
output_dim = 1
GNN_layers = (100, 100, 100)
encoder_layers = (100, )
decoder_layers = (100, )
device = "cuda:0"
optimizer = {"name": "adam",
             "params": {"lr": 1e-2}}
epochs = 100


[CONFIG2]
input_dim = 4
hidden_dim = 100
output_dim = 1
GNN_layers = (100, 100, 100)
encoder_layers = (100, )
decoder_layers = (100, )
device = "cuda:0"
optimizer = {"name": "adam",
             "params": {"lr": 1e-2}}
epochs = 100

[CONFIG3]
input_dim = 4
hidden_dim = 100
output_dim = 1
GNN_layers = (100, )
encoder_layers = (100, )
decoder_layers = (100, )
device = "cuda:0"
optimizer = {"name": "adam",
             "params": {"lr": 1e-2}}
epochs = 100

[CONFIG4]
input_dim = 4
hidden_dim = 100
output_dim = 1
GNN_layers = (100, 100)
encoder_layers = (100, )
decoder_layers = (100, )
device = "cuda:0"
optimizer = {"name": "adam",
             "params": {"lr": 1e-2}}
epochs = 100

[CONFIG5]
input_dim = 4
hidden_dim = 100
output_dim = 1
GNN_layers = (100, 100, 100, 100)
encoder_layers = (100, )
decoder_layers = (100, )
device = "cuda:0"
optimizer = {"name": "adam",
             "params": {"lr": 1e-2}}
epochs = 100

[CONFIG6]
input_dim = 4
hidden_dim = 20
output_dim = 1
GNN_layers = (20, 20, 20,)
encoder_layers = (20, )
decoder_layers = (20, )
device = "cuda:0"
optimizer = {"name": "adam",
             "params": {"lr": 1e-2}}
epochs = 100